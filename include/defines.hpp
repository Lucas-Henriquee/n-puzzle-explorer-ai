#pragma once

#include <iostream>      // Para operações de entrada e saída (std::cout, std::cin, std::endl)
#include <cstdlib>       // Para funções gerais como std::exit, std::rand, std::srand
#include <ctime>         // Para funções relacionadas a tempo, geralmente usadas com std::srand (para inicializar geradores de números aleatórios)
#include <vector>        // Para o uso de std::vector
#include <stack>         // Para o uso de std::stack (útil para implementações de DFS ou Backtracking
#include <unordered_set> // Para o uso de std::unordered_set (para rastrear estados visitados)
#include <unordered_map> // Para o uso de std::unordered_map (útil para mapeamentos chave-valor, se necessário)
#include <algorithm>     // Para algoritmos gerais como std::reverse, std::min, std::max, etc.
#include <functional>    // Para o uso de std::function e outros utilitários funcionais (como hash customizado para unordered_set/map)
#include <chrono>        // Para medição de tempo, como std::chrono::steady_clock

using namespace std;